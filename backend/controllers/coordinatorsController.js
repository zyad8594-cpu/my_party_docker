const pool = require('../config/db');
const ApiResponse = require('../utils/apiResponse');

/**
 * Unique Coordinator actions (Empty for now as most are generic CRUD)
 * This controller can be used for coordinator-specific logic in the future.
 */

