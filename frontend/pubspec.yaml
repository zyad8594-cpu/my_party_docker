name: my_party
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # ===============================
  # 1. واجهات المستخدم وتحسيناتها (UI Enhancements)
  # ===============================
  # cupertino_icons: ^1.0.8         # أيقونات بنمط iOS
  google_fonts: ^8.1.0            # دعم خطوط Google
  # flutter_svg: ^2.0.7             # عرض رسوميات SVG
  # dropdown_button2: ^3.0.0        # قائمة منسدلة مخصصة
  fl_chart: ^1.0.0                # رسوم بيانية متقدمة
  # window_manager: ^0.3.3          # التحكم بنافذة التطبيق (دعم سطح المكتب)

  # ===============================
  # 2. خدمات Firebase (Backend)
  # ===============================
  firebase_core: any               # تهيئة Firebase الأساسية
  firebase_auth: ^6.4.0           # التوثيق والمستخدمين
  cloud_firestore: ^6.3.0         # قاعدة البيانات Firestore
  firebase_messaging: any         # الإشعارات الفورية (Push Notifications)

  # ===============================
  # 3. إدارة الحالة والتوجيه (State Management & Routing)
  # ===============================
  get: ^4.7.3                     # إدارة الحالة، التوجيه، وحقن التبعيات
  equatable: ^2.0.8               # مقارنة الكائنات بسهولة (لمكتبة GetX أو BLoC)

  # ===============================
  # 4. تخزين البيانات محلياً (Local Storage)
  # ===============================
  shared_preferences: ^2.5.4      # تخزين بسيط (مفاتيح/قيم)
  # flutter_secure_storage: ^9.0.0  # تخزين آمن للتوكنات والبيانات الحساسة
  # get_storage: ^2.1.1             # تخزين سريع وقائم على GetX
  # path_provider: ^2.1.2           # تحديد المسارات المناسبة (التطبيق، المخبأ، ...)

  # ===============================
  # 5. الشبكات والاتصالات (Networking & Connectivity)
  # ===============================
  dio: ^5.4.0                     # عميل HTTP قوي (بديل عن http)
  connectivity_plus: ^7.1.1       # مراقبة حالة الاتصال بالإنترنت
  socket_io_client: ^3.0.0        # عميل Socket.IO للإشعارات الفورية

  # ===============================
  # 6. التعامل مع الملفات والوسائط (Files & Media)
  # ===============================
  image_picker: ^1.1.0            # اختيار الصور من المعرض أو الكاميرا
  cached_network_image: ^3.3.0    # تحميل وتخزين مؤقت للصور من الشبكة
  file_picker: ^11.0.2            # اختيار أي ملف من الجهاز
  audioplayers: ^6.6.0            # تشغيل الملفات الصوتية
  flutter_ringtone_player: ^4.0.0+4 # تشغيل نغمات النظام
  flutter_system_ringtones: ^0.0.6 # التحكم برنات النظام (للإشعارات)

  # ===============================
  # 7. إنشاء التقارير والطباعة والمشاركة (Reports & Sharing)
  # ===============================
  pdf: ^3.12.0                    # إنشاء ملفات PDF
  printing: ^5.14.3               # طباعة PDF أو مستندات
  excel: ^4.0.1                   # قراءة وكتابة ملفات Excel
  csv: ^6.0.0                     # معالجة ملفات CSV
  share_plus: ^12.0.2             # مشاركة النصوص والملفات مع تطبيقات أخرى
  url_launcher: ^6.3.0            # فتح روابط ورسائل بريدية ورقم الهاتف

  # ===============================
  # 8. أدوات مساعدة (Utilities)
  # ===============================
  intl: ^0.20.2                   # تنسيق الأرقام والتواريخ
  uuid: ^4.5.1                    # توليد معرفات فريدة (UUID)
  flutter_phoenix: ^1.1.1         # إعادة تشغيل التطبيق بالكامل (للمقاومة)
  mysql1:                         # الاتصال المباشر بقاعدة بيانات MySQL (يُستخدم بحذر)

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4 # استخدم أحدث إصدار متوفر

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon2009x1961.png"

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/sounds/
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Cairo
  #     fonts:
  #       - asset: assets/fonts/Cairo-Regular.ttf
  #       - asset: assets/fonts/Cairo-Bold.ttf
  #         weight: 700
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
